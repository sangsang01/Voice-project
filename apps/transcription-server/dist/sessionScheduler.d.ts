import type { EngineEvent, PcmFrame, SessionRequest } from "@voice/transcription-contracts";
import type { StreamingRuntimeSession } from "./runtime.js";
export interface SessionSchedulerOptions {
    request: SessionRequest;
    runtime: StreamingRuntimeSession;
    emit(event: EngineEvent): void;
    now?: () => number;
    setTimer?: typeof setTimeout;
    clearTimer?: typeof clearTimeout;
    decodeIntervalMs?: number;
    maxWindowMs?: number;
    overlapMs?: number;
}
export declare class SessionScheduler {
    private readonly request;
    private readonly runtime;
    private readonly emitEvent;
    private readonly now;
    private readonly setTimer;
    private readonly clearTimer;
    private readonly decodeIntervalMs;
    private readonly maxWindowMs;
    private readonly overlapMs;
    private sequence;
    private ordinal;
    private revision;
    private prompt;
    private speaking;
    private currentFinalized;
    private terminal;
    private cancelled;
    private utteranceStartMs;
    private frames;
    private timer;
    private busy;
    private queued;
    private pump;
    private consecutiveSlowProvisionals;
    constructor(options: SessionSchedulerOptions);
    start(): void;
    push(frame: PcmFrame): void;
    stop(): Promise<void>;
    cancel(): Promise<void>;
    private onSpeechStarted;
    private onSpeechEnded;
    private ensureTimer;
    private scheduleTick;
    private clearIntervalTimer;
    private queueDecode;
    private drain;
    private runDecode;
    private noteProvisionalTiming;
    private buildWindow;
    private trimRetainedAudio;
    private utteranceEndMs;
    private hasUnfinalizedSpeech;
    private emitState;
    private emitEnvelope;
}
